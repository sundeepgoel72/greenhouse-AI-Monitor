# MVP Implementation

The runtime should include:

- scheduler
- Frigate snapshot downloader
- OpenCV ROI metrics
- MQTT publishing
- local snapshot storage

The detailed code can be applied locally if GitHub connector blocks Python file writes.
